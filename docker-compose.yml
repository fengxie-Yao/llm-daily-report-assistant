version: "3.9"

services:
  llm-daily-report:
    build: .
    container_name: llm-daily-report-assistant
    restart: always
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
